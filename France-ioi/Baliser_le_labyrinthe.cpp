#include <bits/stdc++.h>
#define EPSILON 0.000000001
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    freopen("input.txt.txt", "r", stdin);
    freopen("output.txt", "w+", stdout);

    int nbI, nbS;
    cin >> nbI >> nbS;
    vector<int> node[nbI];
    vector<pair<int, int>> edge(nbS);


    return 0;
}